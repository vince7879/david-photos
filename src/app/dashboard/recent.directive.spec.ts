import { RecentDirective } from './recent.directive';

describe('RecentDirective', () => {
  it('should create an instance', () => {
    const directive = new RecentDirective();
    expect(directive).toBeTruthy();
  });
});
